#include<iostream>
#include<cstdio>
#include<cstdlib>
using namespace std;

int t,n;
int arr[1009][1009];  // the graph

inline int scan()
{
  register int n=0;
  register char c;
  c=getchar_unlocked();
  while(c<'0' || c>'9')
    c=getchar_unlocked();
  while( c>='0' && c<='9' )
    {
      n=n*10+c-'0';
      c=getchar_unlocked();
    }
  return n;
}

inline void test(int tt)   // tt is the test case number
{



  
}



int main()
{
  register int i;
  t=scan();
  for(i=1;i<=t;i++)
    test(i);
  return 0;
}
